/**
 * E2E: CRM CRUD — DOC-060 §7 (H-06)
 * Tests lead management, client conversion, engagement tracking.
 *
 * TODO: Implement after authentication mock is available.
 * These tests require an authenticated session (Google OAuth).
 */
import { test, expect } from '@playwright/test';

test.describe('CRM CRUD', () => {
  test.skip('placeholder — requires authenticated session setup', async () => {
    // Future: test lead status transitions
    // Future: test lead-to-client conversion
    // Future: test engagement creation
    // Future: test pipeline Kanban drag
    // Future: test CRM search
  });
});
