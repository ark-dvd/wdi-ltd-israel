/**
 * E2E: Content CMS CRUD — DOC-060 §7 (H-06)
 * Tests creating, reading, updating, and deleting content via the admin panel.
 *
 * TODO: Implement after authentication mock is available.
 * These tests require an authenticated session (Google OAuth).
 */
import { test, expect } from '@playwright/test';

test.describe('Content CMS CRUD', () => {
  test.skip('placeholder — requires authenticated session setup', async () => {
    // Future: test creating a project via admin UI
    // Future: test editing a service
    // Future: test toggling team member visibility
    // Future: test uploading images
  });
});
